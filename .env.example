# DFlow API Endpoints
# Development endpoints (for testing with real capital)
DFLOW_QUOTE_API_URL=https://dev-quote-api.dflow.net
DFLOW_PREDICTION_MARKETS_API_URL=https://dev-prediction-markets-api.dflow.net

# Production endpoints (uncomment when ready for production)
# DFLOW_QUOTE_API_URL=https://quote-api.dflow.net
# DFLOW_PREDICTION_MARKETS_API_URL=https://prediction-markets-api.dflow.net

# DFlow API Key (optional, but recommended for production)
# Get your API key from: https://pond.dflow.net/quickstart/api-keys
# Contact hello@dflow.net for production API keys
DFLOW_API_KEY=your_api_key_here

# Solana Private Key (optional - can also use --keypair option or default Solana CLI location)
# Format: JSON array of numbers, e.g., [1,2,3,...]
# SECURITY: Never store in .env file - use local environment variable instead
# Example: export SOLANA_PRIVATE_KEY='[1,2,3,4,5,...]' (in your shell, not in a file)
# SOLANA_PRIVATE_KEY=[1,2,3,4,5,...]

# Wallet address to use in the track-user-positions recipe
USER_WALLET_ADDRESS=